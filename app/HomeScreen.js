import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const HomeScreen = () => {
    return (
        <SafeAreaView>
            <Text className="text-lg text-center p-5">This is our HomeScreen..🔥</Text>
        </SafeAreaView>
    );
};

export default HomeScreen;
